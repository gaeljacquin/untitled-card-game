# Repo Structure

Top-level layout:
- `apps/api` Elysia backend server with Socket.IO
- `apps/web` React frontend (Vite + React Router)
- `libs/shared` Shared game logic, types, and utilities
- `scripts` Operational scripts (LocalStack, deployment)
